# 📈 Google Trends SEO Scraper

این پروژه یک اسکریپت سبک و مستقل پایتون است که با استفاده از کتابخانه `pytrends`، داده‌های مربوط به جستجوهای ترند گوگل (Google Trends) را بدون نیاز به API Key رسمی استخراج می‌کند. 

این اسکریپت به طور ویژه برای **تسک‌های سئو (SEO) و تولید محتوای اتوماتیک با هوش مصنوعی (AI Agents)** طراحی شده است تا کلمات کلیدی با رشد انفجاری (Breakout) را برای ایده‌پردازی مقالات پیدا کند.

---

## 🚀 ویژگی‌ها

- **بدون نیاز به API Key:** دور زدن محدودیت‌های سرویس‌های پولی.
- **استخراج دوگانه:** دریافت همزمان لیست «کلمات پرجستجو» (Top) و «کلمات با رشد بالا» (Rising).
- **بهینه‌شده برای وب فارسی:** تنظیمات اختصاصی برای زبان فارسی (`fa-IR`).
- **خروجی استاندارد:** ارائه داده‌ها در قالب JSON تمیز، آماده برای تزریق به پلتفرم‌های اتوماسیون مانند n8n.

---

## 🛠 پیش‌نیازها و نصب

برای اجرای این اسکریپت، باید پایتون (نسخه ۳.۶ به بالا) روی سیستم شما نصب باشد.

**۱. نصب کتابخانه‌های مورد نیاز:**
کتابخانه‌های `pytrends` (برای ارتباط با گوگل) و `pandas` (برای پردازش داده‌ها) را نصب کنید:
```bash
pip install pytrends pandas

## 💻 نحوه استفاده (Usage)

برای اجرای این اسکریپت و دریافت ترندها، مراحل زیر را طی کنید:

۱. **تنظیم کلمه کلیدی (Keyword):**
   فایل `test_trends.py` را باز کنید و مقدار متغیر `keyword` را به کلمه یا حوزه دلخواه خود تغییر دهید (مثلاً `"خرید"`، `"آموزش"` یا `"هوش مصنوعی"`).

۲. **اجرای اسکریپت:**
   ترمینال (یا محیط خط فرمان VS Code) را باز کرده و دستور زیر را اجرا کنید:
   ```bash
   python test_trends.py


## ⚙️ توضیحات فنی و پارامترها

این اسکریپت برای دور زدن محدودیت‌ها و دریافت بهترین نتیجه در وب فارسی، با پارامترهای زیر مهندسی شده است:

* **`hl='fa-IR'`**: زبان (Host Language) روی فارسی تنظیم شده تا گوگل نتایج را با دقت بیشتری برای کلمات فارسی بهینه‌سازی کند.
* **`tz=210`**: منطقه زمانی (Timezone) روی ایران (UTC+3:30) تنظیم شده است که معادل آفست ۲۱۰ دقیقه است. این کار باعث می‌شود ترندها دقیقاً بر اساس روز و ساعت ایران محاسبه شوند.
* **`timeframe='today 1-m'`**: بازه زمانی روی **۳۰ روز گذشته** تنظیم شده است. دلیل این کار جلوگیری از دریافت آرایه‌های خالی است (گاهی اوقات در بازه‌های ۲۴ ساعته برای کلمات فارسی دیتای کافی وجود ندارد).
* **حذف پارامتر `geo`**: برای جلوگیری از بلاک شدن درخواست‌ها توسط گوگل (به دلیل آی‌پی یا تحریم‌ها)، موقعیت مکانی محدود به ایران (`geo='IR'`) نشده است؛ زیرا کلمات فارسی ذاتاً نتایج مربوط به کاربران ایرانی را برمی‌گردانند.
* **تفاوت Top و Rising**: اسکریپت هر دو لیست را پردازش و پارس می‌کند:
    * `top_trends`: کلماتی که همیشه حجم جستجوی بالایی دارند (Evergreen).
    * `rising_trends`: کلماتی که اخیراً رشد انفجاری داشته‌اند (Breakout) و بهترین گزینه برای ایده‌پردازی محتوا و سئو هستند.

---

## 📊 نمونه خروجی

خروجی اسکریپت یک آبجکت JSON استاندارد است که به راحتی در ابزارهای اتوماسیون (مانند n8n) قابل پردازش است. نمونه خروجی برای کلمه کلیدی `قیمت`:

```json
{
    "keyword": "قیمت",
    "top_trends": [
        {
            "query": "قیمت طلا",
            "value": 100
        },
        {
            "query": "قیمت دلار",
            "value": 98
        }
    ],
    "rising_trends": [
        {
            "query": "قیمت آیفون ۱۶ پرومکس",
            "value": 250
        },
        {
            "query": "قیمت تتر امروز",
            "value": 180
        }
    ]
}
